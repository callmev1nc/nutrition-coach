import type { NextConfig } from "next";

// Note: lucide-react, recharts, and date-fns are optimized automatically by
// Next.js 16 (see docs/.../optimizePackageImports.md), so they don't need to be
// listed under experimental.optimizePackageImports.
const nextConfig: NextConfig = {
  reactStrictMode: true,
};

export default nextConfig;
